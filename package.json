{
  "name": "spyglass",
  "version": "2.5.12",
  "description": "A tool for checking and fixing color contrast on web pages",
  "type": "module",
  "scripts": {
    "build": "node extract-apca-lut.js && node build.js",
    "build:chrome": "TARGET=chrome node build.js",
    "build:firefox": "TARGET=firefox node build.js",
    "package": "node package.js",
    "package:chrome": "TARGET=chrome node package.js",
    "package:firefox": "TARGET=firefox node package.js"
  },
  "dependencies": {
    "apca-w3": "^0.1.9",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
